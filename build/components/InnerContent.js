/** @jsx React.DOM */
var React = require('react');var Text = require('./Text');var Placeholder = require('./Placeholder');var TagPair = require('./TagPair');var InnerContent = React.createClass({  render: function () {    var underscore = '_';    var classId = this.props.classId || 'no-id';    var content = (this.props.content||[]).map(function(content, index){      if (content.type === 'text') {        return (<Text key={index}                      classId={content.type + underscore                        + index + underscore                        + classId}                      text={content.text}                      />);      }      if (content.type === 'placeholder'){        return (<Placeholder key={index}                             classId={content.type + underscore                                 + index + underscore                                 + classId}                             displayText={content.displayText} />)      }      if (content.type === 'tagpair') {        return (<TagPair key={index}                         classId={content.type + underscore                              + index + underscore                              + classId}                         startTagDisplayText={content.startTagDisplayText}                         endTagDisplayText={content.endTagDisplayText}                         childNodes={content.childNodes}/>)      }    });    return (          <div className={this.props.type}>            {content}          </div>      )  }});module.exports = InnerContent;