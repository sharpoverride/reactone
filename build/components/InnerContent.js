/** @jsx React.DOM */
var React = require('react');var Text = require('./Text');var Placeholder = require('./Placeholder');var TagPair = require('./TagPair');var InnerContent = React.createClass({  render: function () {    var content = (this.props.content||[]).map(function(content){      if (content.type === 'text') {        return (<Text text={content.text} />);      }      if (content.type === 'placeholder'){        return (<Placeholder displayText={content.displayText} />)      }      if (content.type === 'tagpair') {        var inlineContent = <InnerContent        type='tag-pair'        content={content.childNodes} />;        return (<TagPair startTagDisplayText={content.startTagDisplayText}                         endTagDisplayText={content.endTagDisplayText}                         inlineContent={inlineContent}/>)      }    });    return (          <div className={this.props.type}>          inner content {this.props.type} {content}          </div>      )  }});module.exports = InnerContent;