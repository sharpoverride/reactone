/** @jsx React.DOM */
var React = require('react');var Tracker = require('../Tracker');var Text = React.createClass({  componentDidMount: function () {    var i = 0,        length,        splitByUnderscore,        id = this.props.classId,        trackedText = {};    if (id.indexOf('no-id') > -1 ) {      return;    }    splitByUnderscore = id.split('_');    length = splitByUnderscore.length;    for(; i < length; i+=2) {      trackedText[splitByUnderscore[i]] = splitByUnderscore[i+1];    }        Tracker.Text.push(trackedText);    console.log('component did mount', this.props.classId, splitByUnderscore);  },  render: function() {    return (      <span className="text">      {this.props.text}      </span>    )  }});module.exports = Text;